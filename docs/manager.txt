Service: org.automotive.message.broker
Interface: org.automotive.Manager
Object Path: /

Methods:

	Array{ObjectPath}	findObject(string property)

		returns list of object paths that provide the given property.

	Array{int32}	zonesForProperty(string property)

		returns list of zones for a given property.

	ObjectPath	findPropertyForZone(string property, int32 zone)

		returns object path which provides the given property in the given zone.


